export default function (context) {
    // if (!context.app.context.app.$cookies.get('token')) {
    //     return context.redirect('/login')
    // }
    // console.log(context.app)
}

// is there any way to get window object here